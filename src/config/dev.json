{
  "stage": "dev",
  "database": {
    "connection": "postgres://postgres:password@localhost:5432/incidence_management",
    "x-connection": {
      "dialect": "sqlite",
      "storage": "./data/indicident_management.db"
    }
  },
  "security": {
    "jwt": {
      "secretKey": "this is the secret key",
      "accessToken": {
        "expiration": "1h"
      }
    }
  }
}
